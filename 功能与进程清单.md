# 功能与进程清单

> 个性化饮食干预课题 — 服务器端研究系统（`gutbrainaxis-health-stack`）
> 版本：2026-09-16 **商用级完善版** ｜ 安装根目录以 `$APP_BASE` 表示（默认 `/opt/gutbrainaxis`）
>
> 本清单逐条列出**功能**与**进程/单元**。凡标注「新增」者为本次商用级完善新增；
> 标注「修正」者为行为/配置发生变化。
>
> **计数口径**：本文件所有计数均与仓库实际文件逐一对应，可用下列命令复核：
> ```bash
> ls systemd/*.service | wc -l     # 常驻+oneshot 服务单元
> ls systemd/*.timer   | wc -l     # 定时器
> ls services/*/ -d    | wc -l     # 服务目录
> ```

---

## 一、功能清单

### A. 问卷评估链路（量表）

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| A1 | 问卷数据拉取（定时兜底） | 从 wj.sjtu.edu.cn 增量拉取量表答卷 | `services/sjtu_survey_pro/survey_sync_cron.py` | 问卷公共 API、`survey_data.db` | `survey_platforms.scale.*`、`schedule.survey_sync_minutes` |
| A2 | 计分核心（10 量表 + BMI） | 按**原生编码口径 3.0-native-options** 计算 GAD-7/PHQ-9/PSS-14/PSQI/GSRS/VSI/DEBQ×3/WHOQOL×4/IPAQ-S/BMI；矩阵题按 `optionN` 序号、单选题按实测真实 label；**未识别即报错**（`UnmappedAnswer`） | `services/sjtu_survey_pro/survey_scoring.py`（权威副本 `survey_scoring_native.py`） | 无（纯函数） | `app.scoring_version` |
| A3 | 报告渲染 | 生成个人 HTML 报告 + 同侪百分位正态曲线 | `survey_analysis.py`、`population_charts.py` | `population_stats*.json` | `app.scoring_version` |
| A4 | 个人反馈页面 | `GET /report?user&quest&answer` 返回**仅该作答**的报告页；其他路径 403 | `feedback_server.py` | A2、A3、DB | `services.survey_feedback.port`、`.port_alt` |
| A5 | 邮件反馈 | 队列化发送个人报告，退避重试 | `email_feedback.py` | SMTP | `smtp.*` |
| A6 | 个人报告触发日志 | 记录每次报告访问（审计） | `feedback_server.py` → `feedback_trigger.log` | 本地文件 | — |
| A7 | 人群基线内置默认数据 | 无本地覆盖时用内置匿名聚合统计画正态曲线/百分位 | `population_stats.default.json` + `tools/build_population_stats.py` | — | `app.scoring_version` |
| A8 | **量表 Webhook（新增）** | 平台提交事件 → **立即**拉取该条答卷 → 计分 → 入邮件队列；与 A1 并存（webhook 为主、定时兜底），幂等去重 | `services/sjtu_survey_pro/survey_webhook_listener.py` | 平台回调、A1 的 `process_row()` | `services.survey_webhook.port`、`webhook.*` |

### B. 饮食记录链路

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| B1 | 饮食问卷拉取 | 定时增量拉取饮食问卷 | `services/diet_survey/diet_sync_cron.py` | 问卷 API、`diet_data.db` | `survey_platforms.diet.*`、`schedule.diet_sync_minutes` |
| B2 | Webhook 接收 | 接收问卷平台提交事件，投递 LLM 任务 | `diet_survey/webhook_listener.py` | DB、队列目录 | `services.diet_webhook.port`、`webhook.*` |
| B3 | LLM 营养分析队列 + **分析调度（修正）** | 文件持久化队列，逐条调用 LLM 生成饮食建议，主/备 API 切换；**新增** `realtime`/`offpeak`/`hybrid` 三种调度模式与可配窗口、节流、并发 | `diet_survey/diet_llm_queue.py` | LLM API、`.llm_queue/` | `analysis.*`、`llm.primary.*`、`llm.backup.*` |
| B4 | 饮食建议页面 | `GET /report?...` 返回个人饮食建议 | `diet_feedback_server.py` | DB | `services.diet_feedback.port` |
| B5 | 饮食邮件反馈 | 发送饮食建议邮件 | `diet_email_feedback.py` | SMTP | `smtp.*` |
| B6 | 营养素解析 | 菜品→营养素映射 | `food_nutrition.csv`、`dish_nutrition_calculated.csv` | — | — |

### C. 运动问卷链路

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| C1 | 运动问卷拉取 | 定时增量拉取运动问卷（仅步行类条目） | `services/exercise_survey/exercise_sync_cron.py` | 问卷 API、`exercise_data.db` | `survey_platforms.exercise.*`、`schedule.exercise_sync_minutes` |
| C2 | 运动数据存取 | 运动记录 CRUD 与聚合 | `exercise_database.py` | DB | `database.exercise_db` |

### D. 数据看板（研究者）

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| D1 | 看板页面 | 量表/饮食/运动/完成度视图 | `services/data_dashboard/app.py` | 三库 | `services.data_dashboard.port` |
| D2 | 数据访问层 | 聚合统计与趋势 | `data_dashboard/data.py` | 三库、`roster.json` | `database.roster` |
| D3 | 交叉分析 | 量表间相关、个人趋势 | `/api/scales/cross/*`、`/api/student/*/trends` | D2 | — |
| D4 | 看板数据导出（既有） | `/api/export/*` 导出 CSV/JSON | `app.py` | D2 | — |
| D5 | **队列运行状况（新增）** | LLM 队列（pending/processing/done/failed/**等待窗口**）、量表与饮食邮件队列（pending/sent/failed）、Webhook 回调健康与重试积压 | `/queues`、`/api/queues`、`/api/export/queues` | B3、A5、A8、`ops.py` | `analysis.*`、`webhook.*` |
| D6 | **依从性时间序列（新增）** | 每人逐日提交热力图 + 分链路（量表/饮食/运动）完成率趋势 + 整体趋势 | `/adherence`、`/api/adherence`、`/api/export/adherence` | 三库、roster | `database.*` |
| D7 | **数据质量（新增）** | 缺失项、异常值（超出量程）、重复提交、未映射条目、模拟数据残留 | `/quality`、`/api/quality`、`/api/export/quality` | 三库、`ops.py` | — |
| D8 | **系统健康（新增）** | 聚合各服务 `/healthz` + 端口监听 + 数据库状态 | `/system`、`/api/system/health`、`/api/export/system` | 全服务 | `services.*.port`、`selfcheck.*` |

### E. 管理控制台

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| E1 | 认证与会话（修正） | 口令登录、**口令强度校验**、签名 Cookie、首次强制改密、**持久化登录锁定** | `admin_console/auth.py`、`app.py` | `console.env`、`services/common/lib_ratelimit.py` | — |
| E2 | 配置管理 | 读写控制台配置，带校验与恢复默认 | `admin_console/config_manager.py` | `console.env` | — |
| E3 | 进程管理 | 对白名单服务做 start/stop/restart/enable/disable | `admin_console/service_manager.py` | systemd | — |
| E4 | 端口映射 | 改端口/反代 → 冲突检测 → 预览 → `nginx -t` → reload | `admin_console/port_manager.py` | nginx | `services.*.port`、`site.*` |
| E5 | 健康巡检页 | 服务探活 / 日志尾 / DB 检查 | `admin_console/health_check.py` | 三库 | — |
| E6 | 系统自检页 | 复用 `tools/selfcheck.py` 同一后端 | `/selfcheck`、`/api/selfcheck` | `tools/selfcheck.py` | `selfcheck.*` |
| E7 | 审计日志 | 所有写操作留痕 | `admin_console/audit.py` | `$APP_BASE/logs/console_audit.log` | — |
| E8 | 健康探针 | `GET /healthz`、`/health` | `app.py` | 配置目录可写 | — |
| E9 | **调度页（新增）** | 分组展示**全部 55 个时间项**（键名/当前值/单位/作用/生效方式）+ 窗口与静默实时状态 + timer 渲染预览；支持修改、变更预览、写前自动备份、审计 | `/api/schedule`、`/api/schedule/preview`、`admin_console/schedule_manager.py` | `tools/appconfig.py` | `schedule.*`、`analysis.*`、`webhook.*`、`alert.*`、`selfcheck.*` |

| E10 | **应用配置页（新增）** | 按命名空间列出**全部 180 个个性化配置项**（类型/当前值/默认值/必填/生效方式/注入的环境变量名），支持修改、变更预览、恢复默认、写前自动备份、写审计；**密钥类只显示是否已配置**（不回显/不可改，只在 secrets.env 维护） | `/api/appconfig`、`admin_console/appconfig_manager.py` | `tools/appconfig.py` | `config/app.yaml` 全量键 |

### F. 定时任务与运维

| # | 功能 | 作用 | 入口 | 依赖 | 关键配置项 |
|---|---|---|---|---|---|
| F1 | 每日报告 | 每日汇总问卷/饮食统计并邮件发送（支持抄送） | `services/feedback/daily_report.py` | SMTP、三库 | `schedule.daily_report_at`、`smtp.cc_email` |
| F2 | 健康巡检与告警（修正） | 检查数据滞后、进程、端口、**Webhook 回调时效**、LLM 队列堆积；**分级 + 静默时段 + 冷却** | `scripts/health_monitor.py` | DB、SMTP | `alert.*`、`schedule.health_monitor_minutes` |
| F3 | 告警发送（修正） | 通用告警邮件发送器，支持 `--severity` / `--force`、重试、静默落盘 | `scripts/send_alert.py` | SMTP | `alert.*`、`smtp.send_retry_*` |
| F4 | 本地备份 | 备份各库 + 配置 + systemd 单元，按份数保留 | `scripts/backup.sh` | — | `backup.dir`、`backup.retention`、`backup.timeout_minutes` |
| F5 | 远端备份 | rclone 上传备份到远端 | `scripts/rclone-backup.sh` | rclone | `backup.remote_enabled`、`backup.rclone_remote` |
| F6 | 建库（空 schema） | 创建 3 个库的空 schema | `scripts/init_db.py` | — | `database.*` |
| F7 | 系统自检（修正） | 新增 webhook 检查组与配置一致性检查 | `tools/selfcheck.py` | 全部 | `selfcheck.*` |
| F8 | 统一配置校验/渲染（修正） | app.yaml 校验 + 渲染 systemd 环境文件/timer 周期 + **悬空键门禁** + 窗口状态 | `tools/appconfig.py` | PyYAML、`services/common/lib_schedule.py` | `RESEARCH_APP_CONFIG` |
| F9 | 数据导出 | 一键导出 CSV/JSON，可匿名化 | `tools/export_data.py` | 三库 | `database.*` |
| F10 | 升级迁移 | schema/配置/单元迁移，幂等可预演 | `tools/migrate.py` | 三库 | `database.*` |
| F11 | 模拟数据注入/清除 | 端到端功能验证；测后清除并校验 | `tools/seed_mock_data.py`、`tools/clear_mock_data.py` | 全部 | — |
| F12 | 人群基线生成 | 从原始问卷生成匿名聚合默认数据 | `tools/build_population_stats.py` | 原始问卷 + 权威条目库 | — |
| F13 | 脱敏扫描 | 发布前扫描真实数据/凭据/PII/不可移植路径 | `tools/scan_sensitive.py` | — | — |
| F14 | 安装/卸载/自检入口 | 一键安装（幂等、dry-run）、卸载、自检入口 | `install.sh`、`uninstall.sh`、`verify.sh` | 全部 | — |
| F15 | **CI 门禁（新增）** | 7 步：bash -n / py_compile / 测试 / 悬空键 / 示例校验 / 脱敏 / 可选 dry-run 幂等 | `ci.sh` | 全部 | — |
| F16 | **单元/集成测试（新增）** | 50 项（窗口调度 / webhook / 配置一致性），含真实 HTTP 集成 | `tests/` | — | — |
| F17 | **时间项文档生成（新增）** | 从配置生成《时间项配置总表》，支持 `--check` 防漂移 | `tools/gen_time_config_doc.py` → `docs/时间项配置总表.md` | `tools/appconfig.py` | — |
| F18 | **运维手册与贡献指南（新增）** | 日常巡检、故障处置、密钥轮换、上线检查单；开发约定与发布门禁 | `docs/运维手册.md`、`CONTRIBUTING.md`、`CHANGELOG.md` | — | — |

| F19 | **计分口径一致性验证（新增）** | 同一份输入同时跑「程序内实现」与「课题权威条目库」，四层断言（L1 条目级/L2 量表级/L3 常量级/L4 缺失与严格模式）逐条相同；纳入自检 `parity` 组 | `tools/verify_scoring_parity.py` | `14_评分标准/03_条目库_修正/survey_scoring_native.py`（只读；生产无此文件时优雅 skip） | `AUTHORITY_SCORING_PATH` |
| F20 | **可配置项总清单生成（新增）** | 从 schema+example 生成《可配置项总清单》（180 项，含生效方式与注入的环境变量名），`--check` 防漂移 | `tools/gen_config_doc.py` → `docs/可配置项总清单.md` | `tools/appconfig.py` | — |

### H. 运行期公共设施

| # | 功能 | 作用 | 入口 |
|---|---|---|---|
| H1 | 优雅停机 | SIGTERM/SIGINT → 置停机标志 → 回调 → 干净退出 | `services/common/lib_runtime.py` |
| H2 | `/healthz` 统一探针 | 各服务统一 JSON 健康端点（含 DB 可读性） | 各服务入口 |
| H3 | 结构化日志 | JSON 行日志，journald 友好 | `services/common/lib_runtime.py` |
| H4 | 日志轮转 | 按天轮转 + 保留 14 份 + 压缩 | `config/logrotate.research-app` |
| H5 | **时间窗口单一真源（新增）** | 窗口/时区/静默判定；被 appconfig、LLM worker、各 sync、告警、看板、控制台共用 | `services/common/lib_schedule.py` |
| H6 | **持久化文件队列（新增）** | 原子领取、幂等 upsert、崩溃恢复、死信；被 LLM 队列与 webhook 重试共用 | `services/common/lib_queue.py` |
| H7 | **限流与口令强度（新增）** | 滑动窗口限流 + **持久化**登录锁定 + 口令强度校验 | `services/common/lib_ratelimit.py` |
| H8 | **统一错误与输入校验（新增）** | 对外安全的错误响应、异常统一日志、输入上限校验 | `services/common/lib_errors.py`、`services/common/lib_validate.py` |

| H9 | **研究设计参数单一真源（新增）** | 干预起止/轮次窗口/周次切点/目标完成度/名册规则；带内置默认值（= 原硬编码值），文件缺失不报错 | `services/common/lib_study.py` + `config/study_calendar.example.yaml` |

**功能条目统计**：A 8 + B 6 + C 2 + D 8 + E 10 + F 20 + H 9 = **63 项**
（其中新增 **17 项**、修正 **8 项**）

---

## 二、进程 / 单元清单

### 2.1 服务单元（systemd `*.service`，**13 个**）

| 单元名 | 类型 | 监听端口 | 启动依赖 | 数据落点 | 状态 |
|---|---|---|---|---|---|
| `research-survey-feedback.service` | simple | 8000（+8080 兼容） | network-online | `sjtu_survey_pro/survey_data.db`、`feedback_trigger.log` | 已有 |
| `research-diet-feedback.service` | simple | 8001 | network-online | `diet_survey/diet_data.db`、`logs/feedback_trigger.log` | 已有 |
| `research-diet-webhook.service` | simple | 9876 | network-online | `.llm_queue/`、`logs/webhook.log` | 已有 |
| `research-survey-webhook.service` | simple | **9877** | network-online | `.webhook_queue/` | **新增** |
| `research-diet-llm-queue.service` | simple | —（worker） | network-online、`research-diet-webhook` | `.llm_queue/{pending,processing,done,failed}/`、`logs/llm_queue.log` | 已有 |
| `research-admin-console.service` | simple | 9000 | network-online | `admin_console/config/`、`logs/console_audit.log` | 已有 |
| `research-data-dashboard.service` | simple | 8090 | network-online | 只读三库 + `roster.json` | 已有 |
| `research-survey-sync.service` | oneshot（timer 触发） | — | network-online | `.email_queue/sync_cron.log` | 已有 |
| `research-diet-sync.service` | oneshot（timer 触发） | — | network-online | `.email_queue/sync_cron.log` | 已有 |
| `research-exercise-sync.service` | oneshot（timer 触发） | — | network-online | `exercise_survey/logs/sync_cron.log` | 已有 |
| `research-health-monitor.service` | oneshot（timer 触发） | — | network-online | `.health_state.json` | 已有 |
| `research-rclone-backup.service` | oneshot（timer 触发） | — | network-online | `backups/`、`/var/log/rclone-backup.log` | 已有 |
| `research-daily-report.service` | oneshot（timer 触发） | — | network-online | 邮件；读三库 | 已有 |

> 合计 **7 个 `Type=simple` 常驻 + 6 个 `Type=oneshot`（timer 触发）= 13 个服务单元**。

### 2.2 定时器（systemd `*.timer`，**6 个**）

| 单元名 | 周期（默认） | 对应 service | 配置项 | 状态 |
|---|---|---|---|---|
| `research-survey-sync.timer` | 每小时 `*:0/60` | `research-survey-sync` | `schedule.survey_sync_minutes` | 已有 |
| `research-diet-sync.timer` | 每 15 分钟 | `research-diet-sync` | `schedule.diet_sync_minutes` | 已有 |
| `research-exercise-sync.timer` | 每 15 分钟 | `research-exercise-sync` | `schedule.exercise_sync_minutes` | 已有 |
| `research-health-monitor.timer` | 每 30 分钟 | `research-health-monitor` | `schedule.health_monitor_minutes` | 已有 |
| `research-daily-report.timer` | 每日 08:30 | `research-daily-report` | `schedule.daily_report_at` | 已有 |
| `research-rclone-backup.timer` | 每日 03:00 | `research-rclone-backup` | `schedule.backup_at` | 已有 |

> **单位总数：13 service + 6 timer = 19 个 systemd 单元。**
> timer 周期由 `config/app.yaml → schedule.*` 决定；`install.sh` 调用
> `tools/appconfig.py --render-timers /etc/systemd/system` 生成
> `/etc/systemd/system/<unit>.timer.d/10-schedule.conf`
> （`OnCalendar` / `RandomizedDelaySec` / `AccuracySec` / `Persistent`）。

### 2.3 端口占用汇总（**6 个监听端口**）

| 端口 | 服务 | 可配置键 |
|---|---|---|
| 8000 / 8080 | 问卷反馈（主 / 兼容） | `services.survey_feedback.port` / `.port_alt` |
| 8001 | 饮食反馈 | `services.diet_feedback.port` |
| 8090 | 数据看板 | `services.data_dashboard.port` |
| 9000 | 管理控制台 | `services.admin_console.port` |
| 9876 | 饮食 Webhook | `services.diet_webhook.port` |
| 9877 | **量表 Webhook** | `services.survey_webhook.port` |

### 2.4 运行期数据落点总表

| 路径 | 内容 | 保留策略 |
|---|---|---|
| `$APP_BASE/sjtu_survey_pro/survey_data.db` | 问卷答卷、量表分、邮件日志 | 随备份保留 `backup.retention` 份 |
| `$APP_BASE/sjtu_survey_pro/.email_queue/` | 邮件队列与同步日志 | 日志按天轮转 14 份 |
| `$APP_BASE/sjtu_survey_pro/.webhook_queue/` | Webhook 状态与重试队列 | 手动清理 `*.dead` |
| `$APP_BASE/diet_survey/diet_data.db` | 饮食记录与建议 | 随备份保留 |
| `$APP_BASE/diet_survey/.llm_queue/` | LLM 任务队列与调度状态 | 手动清理 `done/` |
| `$APP_BASE/exercise_survey/exercise_data.db` | 运动记录 | 随备份保留 |
| `$APP_BASE/logs/` | 安装/控制台/巡检/备份日志、`alerts_quiet.log` | logrotate 按天 14 份 |
| `$APP_BASE/backups/` | 备份 tar.gz | `backup.retention` 份 |
| `/etc/research-app/app.yaml` | **统一配置（单一真源）** | 不轮转；变更前自动备份 `.bak.<ts>` |
| `/etc/research-app/secrets.env` | 真实密钥（0600） | **绝不入库** |
| `/etc/research-app/env` | 由 app.yaml 渲染的 systemd 环境文件（0600） | 自动生成 |

---

## 三、一致性校验（本文件 vs 实际文件）

| 检查 | 结果 |
|---|---|
| `systemd/*.service` 数量 = 2.1 表行数 | ✅ 13 = 13 |
| `systemd/*.timer` 数量 = 2.2 表行数 | ✅ 6 = 6 |
| 每个服务单元 `ExecStart` 指向的文件是否随包安装 | ✅ 13/13（`services/`、`scripts/`、`tools/`、`docs/` 均随包安装） |
| 每个单元 `Documentation=` 指向的文档是否存在 | ✅ 13/13 + 6/6 |
| 控制台白名单是否覆盖全部单元 | ✅ 覆盖（含 `survey_webhook`/`daily_report`/`rclone_backup`） |
| 每个业务库是否有建库入口 | ✅ 4/4（`scripts/init_db.py` + 各服务 `init_db()`） |
| 每个可配置项是否在 `config/app.schema.json` 中声明 | ✅ **151 键全覆盖**，且与 `app.yaml.example` **无悬空键** |
| 时间项是否在 schema+example+文档三处一致 | ✅ **55 项**（`gen_time_config_doc.py --check` 通过） |
| 每个监听端口是否有独立配置键 | ✅ 6/6 |
| 是否有测试与 CI | ✅ `tests/` **63 项**、`ci.sh` **8 步** |
| 计分口径是否与课题权威条目库逐条一致 | ✅ `verify_scoring_parity.py`：**1771 断言全等**（自检 `parity` 组） |
| 配置是否三方一致（schema↔yaml↔实际读取） | ✅ 180 键无悬空；`config.env_bridge` 0 未声明 |
| 干预时间轴/目标完成度是否已外提可配 | ✅ `study_calendar.yaml` + `lib_study.py`（默认值 = 原硬编码值） |

### 复核命令

```bash
# 单元计数
ls systemd/*.service | wc -l        # 期望 13
ls systemd/*.timer   | wc -l        # 期望 6

# 配置计数与一致性
python3 tools/appconfig.py --check-example-keys         # 期望：无悬空键（151 键）
python3 tools/gen_time_config_doc.py --check             # 期望：时间项文档一致（55 项）

# 质量门禁
bash ci.sh                                               # 期望：CI 全部通过
```
