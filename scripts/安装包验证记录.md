# 安装包验证记录

验证日期: 2026-09-16
环境: macOS (arm64) — 仅语法检查 + dry-run（生产环境需 Ubuntu 22.04/24.04）

## 1. bash -n 语法检查

| 脚本 | 结果 |
|------|------|
| `install.sh` | ✅ OK |
| `uninstall.sh` | ✅ OK |
| `verify.sh` | ✅ OK |
| `scripts/backup.sh` | ✅ OK |
| `scripts/rclone-backup.sh` | ✅ OK |

命令:
```bash
for f in install.sh uninstall.sh verify.sh scripts/backup.sh scripts/rclone-backup.sh; do
  echo -n "$f: "; bash -n "$f" && echo "OK" || echo "FAIL"
done
```

## 2. python3 -m py_compile 语法检查

| 脚本 | 结果 |
|------|------|
| `scripts/init_db.py` | ✅ OK |
| `scripts/send_alert.py` | ✅ OK |

命令:
```bash
python3 -m py_compile scripts/init_db.py && echo "init_db.py: OK"
python3 -m py_compile scripts/send_alert.py && echo "send_alert.py: OK"
```

## 3. Dry-run 测试（`--dry-run --prefix=/tmp/test_install`）

### 第 1 次 — 首次运行
- 退出码: 0 ✅
- 平台检测: 跳过（macOS dry-run 模式）
- 步骤覆盖: 全部 11 步 ✅
  - 平台检测 ✅
  - apt 依赖安装 ✅
  - venv 创建 ✅
  - services 复制 ✅
  - pip 依赖安装 ✅
  - init_db 初始化 ✅
  - 配置文件生成 ✅
  - systemd 单元（10 services + 4 timers）✅
  - Nginx 配置安装 ✅
  - 探活预览 ✅
  - 自检跳过（dry-run）✅

### 第 2 次 — 幂等验证（连跑两次）
- 退出码: 0 ✅
- 与第 1 次输出完全一致 ✅
- 所有步骤无重复动作/报错 ✅
- 符合幂等设计要求 ✅

## 4. systemd 单元完整性

| 单元名称 | 类型 | 端口 | 触发方式 |
|----------|------|------|----------|
| `research-survey-feedback.service` | 常驻 | :8000 + :8080 | systemd enable --now |
| `research-diet-feedback.service` | 常驻 | :8001 | systemd enable --now |
| `research-diet-webhook.service` | 常驻 | :9876 | systemd enable --now |
| `research-diet-llm-queue.service` | 常驻 | 无 | systemd enable --now |
| `research-exercise-sync.service` | oneshot | 无 | timer 触发 (每 15min) |
| `research-exercise-sync.timer` | timer | - | - |
| `research-survey-sync.service` | oneshot | 无 | timer 触发 (每小时) |
| `research-survey-sync.timer` | timer | - | - |
| `research-admin-console.service` | 常驻 | :9000 | systemd enable --now |
| `research-data-dashboard.service` | 常驻 | :8090 | systemd enable --now |
| `research-health-monitor.service` | oneshot | 无 | timer 触发 (每 30min) |
| `research-health-monitor.timer` | timer | - | - |
| `research-rclone-backup.service` | oneshot | 无 | timer 触发 (每天 3:00) |
| `research-rclone-backup.timer` | timer | - | - |

## 5. 文件产出清单

```
12_部署包_可发布/
├── scripts/
│   ├── install.sh          # 一键安装脚本（幂等，dry-run 支持）
│   ├── uninstall.sh        # 卸载脚本（--purge 删除数据）
│   ├── verify.sh           # 自检脚本（✅/❌ 清单）
│   ├── backup.sh           # 本地备份（保留 N 份）
│   ├── rclone-backup.sh    # 远程备份（rclone → OneDrive）
│   ├── init_db.py          # 数据库空 schema 初始化
│   ├── health_monitor.py   # 系统健康监控（脱敏版）
│   ├── send_alert.py       # 告警邮件发送（脱敏版）
│   └── 安装包验证记录.md
├── systemd/
│   ├── research-survey-feedback.service
│   ├── research-diet-feedback.service
│   ├── research-diet-webhook.service
│   ├── research-diet-llm-queue.service
│   ├── research-exercise-sync.service + .timer
│   ├── research-survey-sync.service + .timer
│   ├── research-admin-console.service
│   ├── research-data-dashboard.service
│   ├── research-health-monitor.service + .timer
│   ├── research-rclone-backup.service + .timer
│   └── (共 10 services + 4 timers)
├── nginx/
│   ├── research-app.conf   # 站点模板（7 子域名）
│   └── README.md           # Nginx 配置说明
├── config/
│   ├── env.example         # 环境变量模板（全部占位符）
│   ├── console.env.example # 控制台配置模板
│   └── rclone.conf.example # rclone 远端配置模板
```

## 6. 红线检查

- [x] 无真实数据（init_db.py 只创建空 schema）
- [x] 无真实凭据（全部 `__CHANGE_ME__` / `<...>` 占位符）
- [x] 无写死绝对路径（`INSTALL_ROOT` 变量 + 配置可覆盖）
- [x] 无破坏性 `rm -rf`（uninstall.sh 仅 `--purge` 时删除，需确认）
- [x] 私有文件权限 600
- [x] services/ 目录未修改
