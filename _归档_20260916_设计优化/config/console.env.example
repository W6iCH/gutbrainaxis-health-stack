# ============================================================================
#  管理控制台 - 环境变量配置模板
# ============================================================================
#  本文件供管理控制台（admin_console）读取，若从 /etc/research-app/env 读取
#  且已有对应变量，则本文件可保持默认值。
#  安装时由 install.sh 生成，如首次安装会生成随机 SECRET_KEY。
# ============================================================================

# ── 监听端口 ──────────────────────────────────────────────────────────────
# 必填。管理控制台 HTTP 监听端口。
CONSOLE_PORT=9000

# ── 管理员密码 ────────────────────────────────────────────────────────────
# 必填。控制台登录口令。安装时由 install.sh 生成随机值。
# 首次登录后请立即修改。
CONSOLE_PASSWORD=<ADMIN_PASSWORD>

# ── 会话密钥 ──────────────────────────────────────────────────────────────
# 必填。Flask 会话签名密钥。安装时由 install.sh 生成。
# 改为随机字符串，越长越安全。
SECRET_KEY=<RANDOM_SECRET>
