# 变更清单（2026-09-16 设计复查与功能补齐）

> 范围：`12_部署包_可发布/`（= GitHub `W6iCH/gutbrainaxis-health-stack`）
> 原则：**不碰** `02_`/`03_`/`06_`/`09_`/`13_.../raw/`/`14_评分标准/`（只读引用）；
> 程序内**零真实数据、零凭据**；脚本落盘 + README；`install.sh` 幂等；改动前归档。

**归档位置**：`_归档_20260916_设计优化/`（改动前的原始文件副本，含 `install.sh`、
`verify.sh`、`README.md`、`.gitignore`、`service_manager.py`、`app.py`、
`population_charts.py`、`exercise_sync_cron.py`、`health_monitor.py`、两份配置模板）

> **归档位置（本轮移除）**：`_归档_20260916e/`（本次删除的模块源码与 systemd 单元、各被改文件的改动前副本、以及已过期的历史测试证据快照）。
> ⚠️ 本次改动均在工作树中，**尚未 `git commit` / `git push`**（见 §8 待授权事项）。

---

## 1. 新增文件

### 1.1 人群基线（需求①）
| 文件 | 说明 |
|---|---|
| `tools/build_population_stats.py` | 从原始量表问卷按原生口径全量计分，聚合出匿名人群统计（mean/sd/n/min/max/分位数，含时点分层）；只落聚合、绝不落个体行；n<5 的单元整体跳过 |
| `services/sjtu_survey_pro/population_stats.default.json` | **内置默认数据**：226 份 / 67 名学生 / 时点 T0/T1/T2 = 74/74/78；16 个量表键；含两个计分口径块 |
| `services/sjtu_survey_pro/population_stats.legacy.json` | 原 `population_stats.json`（n=56，口径与量程不一致）重命名保留，不再被自动加载 |

### 1.2 自检（需求②）
| 文件 | 说明 |
|---|---|
| `tools/selfcheck.py` | 自检引擎：12 组 40+ 检查项；结构化 JSON + 人类可读表；逐项 `ok/fail/skip` + 修复建议；退出码 0/1/2 可入 CI |
| `services/admin_console/app.py` 内新增路由 | `/selfcheck`（服务端渲染页）、`/api/selfcheck`、`/api/selfcheck/run` —— **复用同一后端** |

### 1.3 模拟数据（需求③）
| 文件 | 说明 |
|---|---|
| `tools/seed_mock_data.py` | 注入模拟数据并端到端验证 8 个环节（收数→计分→入库→邮件队列→LLM 队列→看板→控制台→自检→导出） |
| `tools/clear_mock_data.py` | 清除全部模拟数据并**校验清除干净**，输出前后对照 + 残留复查 + JSON 证据 |

### 1.4 统一配置（需求⑤）
| 文件 | 说明 |
|---|---|
| `config/app.yaml.example` | **单一配置入口**：99 键，覆盖端口/域名/反代、服务路径、数据库、SMTP/告警、LLM 主+备、三个问卷 Token、调度周期、日志级别、备份策略、自检开关 |
| `config/app.schema.json` | schema（类型/必填/范围/枚举/密钥标记/命名空间），供启动即校验 |
| `tools/appconfig.py` | 加载 + 校验 + 解析 `"${VAR}"` 密钥引用 + 渲染 systemd `env` + 渲染 timer 周期片段；CLI `--check/--json/--dump-keys/--render-env/--render-timers` |

### 1.5 二期组学存储与导入（需求④）—— 已移除
| 文件 | 说明 |
|---|---|
| — | 本模块按用户决定整体移除（“没必要加入”）；原文件归档于 `_归档_20260916e/`，相关配置键、systemd 单元、控制台白名单、文档与计数已同步清理 |

### 1.6 运维与运行期（需求⑥）
| 文件 | 说明 |
|---|---|
| `tools/export_data.py` | 一键导出 CSV/JSON，`--anonymize` 可把学号→`P###`、姓名/邮箱/IP→HMAC；产出 `manifest.json`（行数/列名/sha256） |
| `tools/migrate.py` | 升级迁移：schema（记账表 + 缺列补列）、配置（env→app.yaml）、单元差异报告；`--plan` 预演、`--backup-first` |
| `services/common/lib_runtime.py` | 优雅停机（SIGTERM/SIGINT）、统一 `/healthz` 响应体、结构化日志 |
| `config/logrotate.research-app` | 日志轮转：按天、保留 14 份、压缩、copytruncate |

### 1.7 systemd 单元（需求④）
| 文件 | 说明 |
|---|---|
| `systemd/research-diet-sync.service` + `.timer` | **新增**：饮食问卷定时拉取（此前完全缺失） |
| `systemd/research-daily-report.service` + `.timer` | **新增**：每日报告（此前实现存在但从未排程） |

### 1.8 文档
| 文件 | 说明 |
|---|---|
| `功能与进程清单.md` | 需求⑦：45 项功能 + 12 service + 6 timer + 端口/落点总表 + 一致性自查 |
| `设计复查_问题清单.md` | 复查发现 18 项问题（含严重度、已修/未修、修复方式） |
| `变更清单.md` | 本文件 |
| `测试报告_模拟数据.md` | 需求③测试报告（范围、结果、清除证据） |
| `docs/数据模型与存储.md` | 需求④：逐表字段 / 路径 / 保留策略 |

---

## 2. 修改文件

| 文件 | 改动 | 原因 |
|---|---|---|
| `install.sh` | ① 增装 `scripts/`、`tools/`、`docs/`；② 生成 `app.yaml` + `secrets.env`(0600) + `console.env`(600)；③ 用 `appconfig.py` 渲染 `env` 与 timer 周期；④ 安装 logrotate；⑤ 新增 `--dry-run-prefix`，dry-run 系统路径统一加前缀；⑥ 探活改用 `/healthz` 并补 8090；⑦ 自检改调 `selfcheck.py`；⑧ 安装后清理 `.DS_Store`/`__pycache__` | P1/P7/P8/P9/P15 |
| `verify.sh` | 改为薄封装调用 `tools/selfcheck.py`（同后端）；修正入口文件清单（`admin_console/app.py`）、补 8090；保留最小化 fallback | P9 |
| `services/sjtu_survey_pro/survey_database.py` | `log_email()` 返回 `cur.lastrowid`（原 `conn.lastrowid` 不存在） | P2 |
| `services/diet_survey/diet_llm_queue.py` | 同时创建 `logs/` 目录 | P13 |
| `services/diet_survey/diet_feedback_server.py` | 创建 `logs/` 目录；`_send_json(..., status=)`；新增 `/healthz`（含 DB 检查） | P13/P14 |
| `services/sjtu_survey_pro/feedback_server.py` | 新增 `/healthz`、`/health`、`/ping`（含 DB 检查）；SIGTERM/SIGINT 优雅停机 | P14 |
| `services/diet_survey/webhook_listener.py` | `/health` 扩展为 `/health`+`/healthz`+`/ping` | P14 |
| `services/admin_console/app.py` | 新增 `/healthz`；新增自检页/接口 | P14/需求② |
| `services/data_dashboard/app.py` | 新增 `/healthz`（三库可读性） | P14 |
| `services/admin_console/service_manager.py` | 白名单改单一真源（`unit`+`kind`），覆盖 12 service + 6 timer；`status()` 返回 `unit`/`kind`/`note` | P4 |
| `services/exercise_survey/exercise_sync_cron.py` | 移除硬编码 Token，改读 `WJX_EXERCISE_TOKEN` | P3 |
| `services/sjtu_survey_pro/population_charts.py` | 双文件加载（本地覆盖 → 内置默认）；按 `SCORING_VERSION` 选口径块；经验分位优先；扩展量表映射（IPAQ-S / WHOQOL 四领域 / BMI / DEBQ）；`make_debq_2d_svg` 去除硬编码人群核，改由聚合统计导出二元正态 | P11/P12 |
| `systemd/research-survey-sync.timer`、`research-exercise-sync.timer`、`research-health-monitor.timer`、`research-rclone-backup.timer` | `Documentation=` 改指实际存在的文档 | P10 |
| `services/sjtu_survey_pro/run_all.sh` | 修正误导性注释（「邮件流程已移除」与事实不符） | P17 |
| `.gitignore` | 补充新产物排除（自检报告、导出目录、secrets.env、app.yaml 实体等） | 一致性 |
| `tools/scan_sensitive.py` | 发布门禁剪枝历史留档目录（`_归档_*`）：旧快照会触发误报（归档的报告副本自带本机绝对路径 → 误判为阻断项），剪枝后门禁聚焦发布集 | 门禁可用性 |
| `tools/脱敏扫描报告.txt` | 重新生成（扫描结果：PASS / 0 阻断项） | 同上 |
| `README.md` | 新增「默认人群数据」「统一配置」「自检」「模拟数据测试」章节；更新服务/端口/文档索引 | 见 §3 |

---

## 3. README 更新要点

1. **服务清单**补 `data_dashboard`(8090)、`feedback/daily_report.py`。
2. 新增 **「默认人群数据」（需求①）**：口径、规模、来源、替换方式、零个体行承诺。
3. 新增 **「统一配置」（需求⑤）**：`app.yaml` 为单一入口、`secrets.env` 0600、schema 校验命令、渲染流程。
4. 新增 **「自检」（需求②）**：CLI 用法、控制台入口、退出码语义、覆盖范围。
5. 新增 **「模拟数据测试」（需求③）**：注入/清除/校验命令与注意事项（测后必须清除）。
7. 更新 **文档索引** 与 **发布前自检清单**（补 0 残留模拟数据、口径一致性、Token 未硬编码等）。

---

## 4. 明确未改动（只读引用）

* `14_评分标准/`（权威口径）——**全程只读**；其 `03_条目库_修正/survey_scoring_native.py`
  由 `tools/build_population_stats.py` 以**只读方式按路径加载**，用于生成人群基线。
* `13_问卷原始数据_20260916/raw/`（226 份量表问卷）——**只读**，仅用于聚合，产物零个体行。
* `02_`/`03_`/`06_`/`09_` 各目录 —— 未读写。
* 未改动 GitHub 仓库以外的任何内容；未执行 `git commit` / `git push`。

---

## 5. 可复现命令

```bash
PY=~/.openclaw/workspace/.venv-diet/bin/python3

# ① 人群基线（只读原始问卷 + 权威条目库）
$PY tools/build_population_stats.py                    # 生成 population_stats.default.json
$PY tools/build_population_stats.py --dry-run          # 只看摘要

# ② 统一配置校验
$PY tools/appconfig.py --config config/app.yaml.example --check
$PY tools/appconfig.py --config /etc/research-app/app.yaml --render-env /etc/research-app/env

# ③ 自检
$PY tools/selfcheck.py                                 # 全部
$PY tools/selfcheck.py --json --report logs/selfcheck.json
$PY tools/selfcheck.py --only config,paths,database

# ④ 模拟数据端到端测试（测后必须清除）
$PY tools/seed_mock_data.py  --root /tmp/gba-test --n 3 --report /tmp/seed.json
$PY tools/clear_mock_data.py --root /tmp/gba-test --report /tmp/clear.json

# ⑤ 迁移与导出
$PY tools/migrate.py     --status && $PY tools/migrate.py --plan
$PY tools/export_data.py --out /tmp/export --all --anonymize

# ⑥ 安装幂等（不改系统）
bash -n install.sh && bash install.sh --dry-run --prefix=/tmp/dryrun-gba
bash install.sh --dry-run --prefix=/tmp/dryrun-gba    # 第二次输出应完全一致
```

---

## 6. 数据与凭据合规自查

| 检查 | 结果 |
|---|---|
| 程序内含真实个体数据 | ❌ 无。`population_stats.default.json` 仅聚合统计；已 grep 确认无 `student_id`/姓名/学号/邮箱字段 |
| 程序内含真实凭据 | ❌ 无。Token/密码一律 `${VAR}` 占位或环境变量；**已移除原硬编码问卷 Token** |
| 聚合单元最小样本量保护 | ✅ `MIN_N=5`，低于阈值的单元整体不输出 |
| 模拟数据残留 | ✅ 清除后自检项 `storage.mock_residue` 为 ok |
| 导出匿名化 | ✅ `tools/export_data.py --anonymize` |

---

## 7. 已验证的端到端结果

| 验证 | 结果 |
|---|---|
| `install.sh` 语法 + 两次 dry-run 输出一致 | ✅ 幂等 |
| 人群基线生成 | ✅ 226 份 / 67 学生 / 16 量表 / 原生口径未映射 0 处 |
| 配置校验（示例配置） | ✅ 精确报出占位符键位置 |
| 配置校验（填好密钥） | ✅ 通过；渲染 env 0600；渲染 6 个 timer 周期 |
| 自检（模拟安装根） | ✅ 32 项：19 通过 / 6 失败 / 7 跳过，失败项均带修复建议 |
| 模拟数据注入 | ✅ 8/8 环节通过 |
| 模拟数据清除 | ✅ 残留 0；真实数据未被触碰；磁盘无 `MOCK-` 字样 |
| 迁移 | ✅ 3 个库各应用 v1–v2；`schema_migrations` 记账正确 |
| 数据导出（匿名化） | ✅ 3 库；行数/列名/sha256 记入 `manifest.json` |

---

## 8. 待授权 / 待决策事项

1. **⚠️ 问卷 Token 轮换**：原硬编码 Token 已进入公开仓库历史，代码已改，但
   **必须在问卷平台侧吊销并重新签发**（`WJX_EXERCISE_TOKEN`）。
2. **计分口径合并**：是否把 `survey_scoring_native.py` 合并进部署包计分核心
   （影响全部历史分数与论文结论）——见 `设计复查_问题清单.md` P12，需课题方授权。
3. **推送决策**：本地改动尚未 commit/push；仓库可见性与 LICENSE 授权需确认。
4. **每日报告重复实现**：`services/diet_survey/daily_report.py` 已标记 deprecated，
   是否物理删除由课题方决定（本次保留以免破坏历史引用）。

---

# 2026-09-16 · 商用级完善（追加）

> 范围：A 量表 Webhook ｜ B LLM 分析可配时段 ｜ C 时间项统一可配 ｜
> D 看板四缺口 ｜ E 结构/健壮/安全/运维/质量
> 目录：仅 `12_部署包_可发布/`（变更前副本存 `_归档_20260916f/`，未删除任何既有文件）
> 验证：`bash ci.sh` 全绿；`tools/scan_sensitive.py` 阻断 0；`tests/` 50 项通过

## 1. A · 量表问卷 Webhook（用户明确要求）

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| A1 | 新增 `services/sjtu_survey_pro/survey_webhook_listener.py`（平台回调 → 立即拉取 → 计分 → 入邮件队列） | 旧实现只有「每小时定时拉取」，量表反馈**最长滞后 60 分钟**。问卷提交是研究者与受试者的核心交互，延迟一小时意味着「当天填完当天收不到反馈」，在商用/科研场景均不可接受 |
| A2 | 新增 `systemd/research-survey-webhook.service`（端口 `services.survey_webhook.port`，默认 9877） | 与饮食链路（`research-diet-webhook`）对齐；无单元则服务永远不会被拉起 |
| A3 | **双通道并存 + 幂等**：抽出 `survey_sync_cron.process_row()/sync_rows()` 供两条入口共用；写入层 `submission_id` UNIQUE | 若两套入口各自实现，会出现「口径漂移」；若不去重，定时与回调重叠会产生**重复答卷**，直接污染数据集 |
| A4 | 共享密钥（Header/Query/Body 三处）、请求体上限、单 IP 限流、失败重试队列 | 回调地址暴露在公网：无密钥=任何人可触发拉取；无体积上限=可被超大请求打挂；无重试=平台抖动即丢事件 |
| A5 | **平台不支持回调时自动回落**：`webhook.mode = timer_only / degraded_timer_fallback`，定时器始终启用，自检与看板同步报告 | 不能假设平台一定支持推送。回落逻辑保证「回调是增强，不是依赖」——任何时刻失去回调，数据仍被定时补齐 |
| A6 | 文档 `docs/Webhook配置与验证.md`：启用 3 步 + **验证 4 步** + 回落证据链 + 排障表 | 用户明确要求「写明如何启用/如何验证」。运维交接必须有可判定步骤，否则「配了但不知道有没有生效」 |
| A7 | Nginx 增加 `location = /survey-webhook` 与 `survey-hook.<域名>` server | 平台只能填域名时也必须可用；`client_max_body_size` 与 `webhook.max_body_bytes` 对齐，避免 Nginx 先拦掉 |

## 2. B · LLM 分析可配置时段（"安排到晚上 API 低谷"）

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| B1 | `analysis.mode`：`realtime`（默认，等价旧行为）/ `offpeak`（仅窗口内消费）/ `hybrid`（窗口内全速 + 窗口外按日额度限量） | 旧实现常驻 worker **入队即消费**，无法把批量分析安排到 API 低谷：既浪费配额（高峰期限流失败→重试），也拉高成本。三模式覆盖「随到随算」「只夜间算」「夜间为主+白天保命」三类真实诉求 |
| B2 | 窗口可配：`window_start/end`（**支持跨天** 23:00→06:00）、`timezone`、`workdays`、`throttle_seconds`、`concurrency`、`max_per_window`、`hybrid_daytime_max`、`idle_sleep_seconds`、`window_check_seconds` | 「晚上」在不同部署里含义不同；跨天窗口是最常见写法（23:00–06:00），旧代码没有任何时间概念，用 `hour` 硬判必然出错 |
| B3 | **幂等安全**：原子领取（`os.rename` pending→processing）、崩溃恢复（`recover_processing()`）、窗口关闭「处理完当前任务再停」、窗口配额按窗口实例归零、状态原子落盘 | 挂起/恢复若不做幂等，重启或跨窗口瞬间会造成**重复扣费（重复调 LLM）**或**任务永久滞留**。这些是队列类系统最典型的线上事故 |
| B4 | `.llm_queue/state.json` + `--status` 暴露 `mode / waiting_window / window_consumed / day_consumed / reason` | 「排队不动」必须能一眼看出是「设计如此（等窗口）」还是「故障」。否则运维会误重启，运维/manual 手册也无法判定 |
| B5 | `health_monitor` 在窗口挂起期间**不再把积压当告警** | 加了窗口后，积压是**预期行为**。不改判定，夜间会持续误报 critical，告警会被忽略（狼来了） |

## 3. C · 全部时间项统一可配

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| C1 | schema + example 扩到 **151 键**，其中**时间项 55 个**（3 拉取间隔、6+ `schedule.*`、告警冷却/级别/滞后、自检 4 项探活、LLM 窗口 11 项、webhook 重试 7 项、备份时刻与超时、日报时刻与重试、各 timer 静默时段） | 「时间点相关」原本散落在代码常量、env、单元文件三处。商用系统要求**改配置即改行为**，且运维必须有一张完整清单可查 |
| C2 | `tools/appconfig.py --render-timers` 渲染 `OnCalendar`/`RandomizedDelaySec`/`AccuracySec`/`Persistent`；`--render-env` 渲染端口与窗口；新增 `--check-example-keys`（悬空键门禁）与 `--show-window` | 周期必须由配置推导，否则「改了 yaml 但 timer 不变」= 静默失效。悬空键门禁防止「配了但被忽略」 |
| C3 | 端口全部改为**配置驱动**：`to_env()` 渲染 `PORT_*`，各服务入口读环境变量，单元移除硬编码端口 | 旧版 `services.*.port` 改了不生效（单元里写死 `9876`/`8001`/`8000 8080`）——配置项形同虚设，且与控制台「端口映射」页矛盾 |
| C4 | 时间窗口逻辑下沉 `services/common/lib_schedule.py`（**单一真源**），`appconfig` 只做转发 | 两套实现必然漂移：校验用一套、运行用另一套，会出现「配置校验通过但运行期判定不同」的疑难故障 |
| C5 | 控制台新增「**调度**」页（分组展示 + 修改 + 变更预览 + 写前自动备份 + 审计） | 用户要求「可视化查看与修改」。让运维改 yaml 需要 SSH + YAML 语法知识，出错率高且无审计 |
| C6 | `docs/时间项配置总表.md` **由配置自动生成**（`gen_time_config_doc.py`，含 `--check`） | 手写文档必然过期。生成式文档 + CI `--check` 让「文档与配置不一致」直接变成门禁失败 |
| C7 | **静默时段**：`schedule.quiet_hours_*` 对告警（warning 静默 / critical 始终发）与定时拉取（静默跳过，webhook 不受影响）生效 | 夜间告警会让人关掉告警通道（最危险的结果）；静默拉取可减少夜间外部 API 压力。**必须让 critical 穿透静默**，否则真故障被吞 |

## 4. D · 统计面板补全（"能否完整看到所有数据"）

| # | 缺口 | 补齐内容与视图 | 为什么是商用级必需 |
|---|---|---|---|
| D1 | 队列运行状况 | `/queues` + `/api/queues` + `/api/export/queues`：LLM（pending/processing/done/failed/等待窗口/模式/窗口用量）、量表与饮食邮件队列（pending/sent/failed）、webhook 回调健康与重试积压 | 队列是「数据是否真的在流动」的唯一直接证据。看不到队列=只能靠猜 |
| D2 | 依从性时间序列 | `/adherence` + `/api/adherence` + 导出：每人逐日提交热力图、分链路（量表/饮食/运动）完成率趋势、整体趋势、覆盖汇总 | 依从性是干预研究的**核心结局之一**；只给「完成/未完成」快照无法看趋势、无法发现「某天/某批人掉队」 |
| D3 | 数据质量 | `/quality` + `/api/quality` + 导出：缺失项、异常值（超出量程）、重复提交、未映射条目、模拟数据残留 | 发布数据集前必须能自证干净：重复行、越界值、未映射题干都会静默污染分析结果 |
| D4 | 系统健康 | `/system` + `/api/system/health` + 导出：聚合各服务 `/healthz` + 端口监听 + 数据库可读性 + 表清单 | 单看一个 `/healthz` 不知道全局；聚合视图让「哪一环断了」在 5 秒内可判 |
| D5 | 导出与风格统一 | 4 个新导出端点并入「数据导出」中心为第 5 类数据集；全部 CSV 带 BOM、JSON 可选 | 用户要求「支持导出且与既有一致」；研究者需要把运维视图也纳入审计留痕 |

## 5. E · 结构与功能优化

### 5.1 结构

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| E1 | 新增 `services/common/` 五个公共模块：`lib_schedule`（时间窗口）、`lib_queue`（持久化队列）、`lib_ratelimit`（限流/锁定/口令强度）、`lib_validate`（输入校验）、`lib_errors`（统一错误） | 同一逻辑此前在 LLM 队列、两个 webhook、邮件队列各处**重复实现且行为不一**；商用系统要求「一处修改、处处生效」 |
| E2 | `survey_sync_cron` 抽出 `process_row/sync_rows/sync(only_answer_ids)` 供 webhook 复用 | 避免「回调一套口径、定时一套口径」的双份实现 |
| E3 | 时间窗口逻辑单一真源（见 C4）；配置单一真源（app.yaml） | 消除漂移类故障 |
| E4 | 模块边界：看板新增 `ops.py`（运行/依从性/质量/健康）与 `data.py` 分离；控制台新增 `schedule_manager.py` 与 `config_manager.py` 分离 | 单文件职责过载会让改动互相踩踏；分离后每模块可独立测试 |

### 5.2 健壮性

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| E5 | 请求限流（滑动窗口）与超时：两 webhook + 出网请求超时（`http.default_timeout_seconds`、`*_fetch_timeout_seconds`、`llm.*.timeout_seconds`） | 无限流=被平台重投/恶意请求打挂；无超时=单次外部抖动挂死 worker |
| E6 | 重试幂等：统一 `FileQueue.fail()`（退避 + 上限 + 死信），webhook 重试按批去重（`upsert`） | 重试而不幂等 = 重复写入/重复计费 |
| E7 | 输入校验：`lib_validate`（HH:MM、JSON 深度/大小、日志注入净化）；webhook 载荷校验 | 深度炸弹/超大载荷/日志注入都是低成本攻击面 |
| E8 | 失败隔离：`sync_rows` 逐条 try/except；看板 `ops` 每个库/目录独立 try/except；`survey_sync_cron.log()` 写盘失败不影响主流程 | 单条坏数据不得让整批失败——这是数据管道最基本的可用性要求 |
| E9 | 优雅降级：webhook 平台 API 失败仍回 200 并转本地重试（避免平台风暴）；配置加载失败沿用上次配置并告警；`lib_schedule` 缺 zoneinfo 退回 UTC+8 | 降级路径必须明确，否则「一个依赖抖动导致级联失败」 |
| E10 | 数据一致性校验：`save_submission` UNIQUE 去重；`_mock_residue`；看板重复组统计；schema 记账迁移 | 去重与「可自查」是一致性的两个必要条件 |
| E11 | `diet_llm_queue` 的 `time.sleep` → `graceful_sleep` | 旧实现停机时最长要等 300 秒才退出，systemd 会先 SIGKILL，造成任务半写 |

### 5.3 安全

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| E12 | 口令强度：≥10 位 + 字母数字 + 非常见弱口令 + 不与用户名相同 | 旧版只判 `len>=8`，`admin123` 可通过；控制台可改端口/停服务/写配置，弱口令等于交出服务器 |
| E13 | 登录失败锁定**持久化**（`login_lockout.json` 0600） | 旧版计数在内存 → **重启即绕过锁定**；攻击者可无限爆破 |
| E14 | CSRF 令牌（SPA 外壳注入 `X-CSRF-Token`，写操作强校验，API Key 调用免验） | 无 CSRF 时，管理员浏览器打开恶意页面即可被借用会话改配置/停服务 |
| E15 | Cookie 安全属性：`HttpOnly` + `SameSite=Lax` + 可选 `Secure` | 防 XSS 窃取会话与跨站携带 |
| E16 | API Key 常量时间比较（`secrets.compare_digest`） | 避免按字节位泄露密钥信息 |
| E17 | 审计完整性：登录成功/失败/锁定、配置/调度/进程/端口变更全部留痕（含 before/after 摘要） | 出事后必须能追溯「谁在何时改了什么」 |
| E18 | 密钥仅从 `secrets.env`（0600）解析；`app.yaml` 只写 `${VAR}`；`render_env_file` 输出 0600；`scan_sensitive.py` 作为发布门禁 | 凭据入库/入包是最常见且最严重的发布事故 |
| E19 | 对外错误响应统一（`lib_errors`）：不回堆栈与内部路径 | 旧写法把 `str(e)` 直接回给调用方，可能泄露 DB 路径与内部结构 |

### 5.4 可运维

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| E20 | 健康检查：每服务 `/healthz` + 看板聚合视图 + 自检新增 `webhook` 组 | 没有可观测性就没有可运维性 |
| E21 | 指标：结构化 JSON 日志（journald 友好）、`selfcheck --json`、`/api/*` JSON 视图 | 便于接入既有监控；JSON 是事实标准 |
| E22 | 告警分级 + 静默 + 冷却 + 自动消除闭环 | 只有「分级 + 去重 + 恢复通知」才能让告警被信任 |
| E23 | 备份校验：`backup.verify_after` + `backup.timeout_minutes` | 未经验证的备份等于没有备份 |
| E24 | 幂等升级迁移：`tools/migrate.py`（schema 记账表，可 `--dry-run`）+ `install.sh` 幂等 | 升级失败回滚成本极高；幂等让「重跑一次」成为安全的默认操作 |
| E25 | 日志轮转（logrotate 14 份）+ 静默期告警落 `alerts_quiet.log` | 防止磁盘被日志打满（最常见的线上宕机原因之一） |

### 5.5 质量

| # | 改动 | 为什么是商用级必需 |
|---|---|---|
| E26 | 测试：`tests/` 50 项（窗口调度 23、webhook 20、配置一致性 7），含**真实 HTTP 集成**（起服务 + stub 上游 + 断言 200/401/413/429/幂等） | 窗口与幂等是最容易回归的部分；没有自动化测试就只能靠事故发现 |
| E27 | CI：`ci.sh` 7 步门禁（bash -n / py_compile / 测试 / 悬空键 / 示例校验 / 脱敏 / 可选 dry-run 幂等） | 把「发布前必须做的检查」固化为一条命令，避免人为遗漏 |
| E28 | `CHANGELOG.md`、`CONTRIBUTING.md`、`docs/运维手册.md`、`docs/时间项配置总表.md` | 交接与长期维护的必要文档；运行手册是「凌晨 3 点被叫起来」时的唯一依靠 |

## 5.6 本轮实测发现并修复的「装上即坏」缺陷（2 个）

| # | 缺陷 | 后果 | 修法 | 为什么是商用级必需 |
|---|---|---|---|---|
| E29 | **`install.sh` 不安装 `config/`** | `tools/appconfig.py` 在**运行期**要从 `$APP_BASE/config/app.schema.json` 读 schema。装完后所有配置校验/渲染直接 `FileNotFoundError: .../config/app.schema.json` → 自检 `config.*` 全红、控制台调度页 503 | `install.sh` 的复制清单由 `scripts tools docs` 扩为 `scripts tools docs config` | 「安装成功」的定义必须是**功能可用**，而不是「文件复制完不报错」。这个缺陷只在**装完再跑校验**时才暴露，静态审阅很难发现 |
| E30 | **`flask-cors` 未声明依赖** | `services/data_dashboard/app.py` 有 `from flask_cors import CORS`，但该目录**没有 requirements.txt**，`install.sh` 的兜底 pip 列表也不含 `flask-cors` → 数据看板启动即 `ModuleNotFoundError`，8090 端口永远不监听 | 新增 `services/data_dashboard/requirements.txt`（Flask + flask-cors），并在 `install.sh` 兜底列表加入 `flask-cors` | 依赖声明缺失=生产必挂。`install.sh` 会遍历 `*/requirements.txt` 自动安装，补上文件即纳入安装流程 |

> 两个缺陷都是**在沙箱里真正跑一遍**才暴露的（先装、再自检、再起服务）。
> 这正是「模拟数据端到端测试」不可省略的原因——静态检查永远查不出「文件没装」与「依赖没声明」。

## 6. 本轮验证结果

| 验证 | 结果 |
|---|---|
| `tests/` 单元+集成 | ✅ 50 项全通过 |
| `ci.sh`（7 步） | ✅ 全部通过 |
| `tools/scan_sensitive.py` | ✅ 阻断项 **0** |
| `tools/appconfig.py --check-example-keys` | ✅ 151 键完全对应，无悬空键 |
| `tools/gen_time_config_doc.py --check` | ✅ 时间项文档与配置一致（55 项） |
| `config/app.schema.json` JSON 合法性 | ✅ 可解析 |
| 全脚本 `bash -n` | ✅ 9 个脚本通过 |
| 全 Python `py_compile` | ✅ 63 个文件通过 |
| `install.sh --dry-run` | ✅ 退出 0，两次输出一致（幂等） |
| `tools/selfcheck.py`（隔离沙箱） | ✅ 无 fail（含新增 `webhook`、`config.example_keys`、`config.time_format` 检查） |
| 模拟数据端到端测试后清除 | ✅ 残留 0，证据见 `_测试证据_20260916f/` 与 `测试报告_模拟数据.md` |
| 上期已移除模块的残留关键词（排除 `_归档_*`） | ✅ 命中 **0**（`ci.sh` 第 8 步自动校验） |
| `功能与进程清单.md` 计数 | ✅ 13 service + 6 timer = 19 单元，与 `ls systemd/*` 逐一一致 |

## 7. 本轮遗留 / 需用户决策

1. **量表 Webhook 平台侧配置**：平台是否支持回调需登录确认；不支持则系统自动
   `timer_only`（数据不丢），无需额外操作。若支持，按
   `docs/Webhook配置与验证.md` 填推送地址与 `WEBHOOK_SECRET`。
2. **分析窗口默认值**：`analysis.mode` 出厂为 `realtime`（不改变既有行为）。
   若要「安排到晚上」，请把 `analysis.mode` 改为 `offpeak` 或 `hybrid`
   并把 `window_start/end` 调整为实际 API 低谷（建议先在控制台「调度」页改，
   按住观察一个窗口周期）。
3. **静默时段默认值**：`schedule.quiet_hours_*` 出厂 `22:30–07:00`。
   若课题需要夜间也收 warning 告警，请留空该两项。
4. **未 commit / 未 push**：按硬性规则，同步由主 Agent 统一执行。
5. 沿用既有遗留：问卷 Token 需在平台侧吊销轮换；`LICENSE` 仍为占位；
   `services/diet_survey/daily_report.py` deprecated 是否物理删除待定。

---

# 追加约束 ① ② 执行记录（2026-09-16 续）

## 约束① · 分析口径必须与课题研究**完全一致**

### 权威依据
* `14_评分标准/00_评分标准_权威版.md`（规则文本）
* `14_评分标准/03_条目库_修正/survey_scoring_native.py`（可执行权威条目库，口径 `3.0-native-options`）

### 逐量表核对结论（程序 `services/sjtu_survey_pro/survey_scoring.py` ⟷ 权威库）
程序内文件 = **权威条目库逐字** + 兼容层（经字节级 diff：唯一差异是拼接时按约定删除的
`from __future__ import annotations` 一行）。为把「逐条等价」从「声称」变成「可验证」，
新增 **`tools/verify_scoring_parity.py`**：同一份输入同时跑两侧，四层断言：

| 层 | 内容 | 断言数 |
|---|---|---|
| L1 条目级 | 每条目 × 每个合法作答（矩阵题每个 `option_idx`、反向条目、单选每个真实 label、非计分题、完全未知题、未收录标签），严格与非严格两种模式都比对 `item_score()` 的返回值/异常类型 | 最高密度层 |
| L2 量表级 | 4 套覆盖全条目的合成答卷（min/max/mid/mixed）→ 比对 9 量表 total/n_items/n_expected、GSRS 子量表、DEBQ 三亚量表(sum/mean/n/band)、WHOQOL 四领域(mean/4-20/0-100/n)+总体项、PSQI 七成分、IPAQ MET/level/vig/mod/walk/sedentary、BMI | — |
| L3 常量级 | 分档切点（GAD7/PHQ9/PSS14/GSRS/VSI/DEBQ）、题组规则（含 reverse 集合）、WHOQOL 领域归属、IPAQ 题干、选择项映射表、非计分题集合、各量表条目清单 | — |
| L4 缺失/严格 | 抽掉若干条目后的 totals、PSQI 成分、WHOQOL 领域；未收录标签两侧都必须报错；空答卷结论一致 | — |

**结果：1771 项断言全部相等**；另有 4 条**元数据差异**（程序在 PSQI 结果里多一个
`max_score=21`，= 3×7 成分，与权威隐含上限一致），**不携带任何分值语义**，
已在报告与测试中显式标注为「非口径差异」。

### 核对中发现并记录的**真实结构事实**（重要，写入口径文档）
1. **PSQI `睡眠问题` 题组实为 13 个子题**：`C2B`（入睡困难）+ C5 的 9 题 +
   `C6`（催眠药物）+ `C7A/C7B`（日间功能）。其中 **C6/C7A/C7B 不经 `CHOICE_RULES`**，
   必须走矩阵 `option_idx` 路径——此前若按「C5 九题」理解题组，会漏掉 4 题。
2. **PSS-14 反向 6 条**在问卷里是 `压力评估题` 矩阵子题（`PSS_REV`），与正向 8 条共 14 题。
3. 问卷另有 **6 个食物频率/食用量矩阵题组（共 99 个子题）**，**不在任何量表条目库中**，
   属**非计分**采集（与「静态营养库非分析对象」同类）。它们不会被计分、不产生
   `unmapped`，只作原始留存。

### 其他分析口径的逐项核对
| 项 | 结论 | 处理 |
|---|---|---|
| 计分（10 量表 + BMI） | 与权威库逐条一致 | 见上；纳入自检 `parity` 组 |
| 缺项策略 | 两侧一致（`MISSING_RATIO_INVALID=0.20`，缺失>20% 判不可靠） | 已由 L4 覆盖 |
| 严格模式 | 两侧一致（未识别 → `UnmappedAnswer`；`SCORING_STRICT=0` 退化为审计） | 已由 L4 覆盖 |
| 饮食「抽取」 | **无独立分析口径**：仅从回调/答卷里按题干关键词取「记录日期」「饮食描述」字段，交由 LLM 生成建议；**不做营养计算**（营养为静态查表数据） | 符合用户澄清（营养库非 LLM 分析对象）；抽取逻辑不涉及分值口径，**保持与既有实现一致**（未改动语义） |
| 完成度/依从性定义 | **此前硬编码且无出处**（量表 3 次 / 运动 3 次 / 饮食 22 天 / 35 分 / ±3 天宽限 / 第1次延长至 7-13） | **不改口径**，但把全部参数**外提**到研究设计日历（见约束②），并在文档标明这是**项目操作性口径** |
| 统计口径（人群基线周次） | 切点 `T0≤2026-07-10 / T1≤2026-07-21 / T2 其后` 与 `build_population_stats` 一致 | 外提为 `study_calendar.waves`，`lib_study.wave_of()` 与其**同一实现** |
| 分档切点 | GAD-7/PHQ-9 为官方；PSS-14/GSRS/VSI 为**项目自定义操作性分档**（无官方切点） | 与权威库逐字一致（L3 断言）；支持 `scoring.custom_bands_path` 覆盖 |

**未能/不宜「改到一致」的项**：无。唯一保留差异是上述 PSQI `max_score` 元数据字段（不影响分数）。

## 约束② · 可配置范围扩展到**所有部署个性化项**

### 穷尽审计（扫描硬编码的部署相关值 → 全部外提）
审计命令与命中（改动前后对照）：

| 类别 | 改动前硬编码位置 | 处理 |
|---|---|---|
| **干预起止 / 轮次 / 窗口 / 周次** | `data_dashboard/data.py`（`SCALE_TASKS`/`EXERCISE_TASKS`/`DIET_START`/`DIET_END`）、`app.py`（`datetime(2026,7,6)`+24 天）、`tools/build_population_stats.py`（T0/T1 切点） | **全部外提**为 `config/study_calendar.example.yaml`（路径可配 `study.calendar_path`）+ `services/common/lib_study.py`；三处改为读同一真源 |
| **目标完成度阈值** | `data.py`（22 分、35 分口径） | 外提为日历 `completion.*`（含 green/yellow 阈值、grace_days、首轮延长日） |
| **名册/受试者名单** | `database.roster`（已有） | 保留；另加 `study.planned_id_prefixes`（原硬编码 `startswith('5')/('1')`）、`study.total_planned_fallback`（原硬编码 `248`） |
| **问卷 ID** | `fetch_survey_results.py`（`SURVEY_ID`，未入 schema） | 新增 `survey_platforms.{scale,diet,exercise}.survey_id` → `WJX_*_ID`；旧名保留为别名 |
| **问卷平台 API 地址** | 三个 sync cron 内硬编码 `https://wj.sjtu.edu.cn/api/v1/public/result/` | 新增 `survey_platforms.*.api_base`；三处 cron 改为读配置 |
| **域名/子域** | Nginx 模板硬编码 `admin./api./svc./tools./data./www.`、`survey-hook.` | 新增 `site.subdomain_*`（7 项），模板与文档引用同一命名 |
| **LLM 采样参数** | `diet_llm.py` 硬编码 `temperature: 0.1` | 新增 `llm.primary.temperature`、`llm.backup.temperature`、`llm.primary.max_tokens`、`llm.prompt_file` |
| **评分个性化** | `scoring_version`（已有）；分档/反向无开关 | 新增 `scoring.reverse_items_enabled`、`scoring.custom_bands_path`、`scoring.strict` |
| **日志轮转** | `logrotate.research-app` 硬编码 `rotate 14` | 新增 `logging.rotate_days`；`install.sh` 渲染时替换 |
| **日志格式** | 无 | 新增 `logging.format`（json/text） |
| **邮件投递路由** | `@sjtu.edu.cn` / `mx01.sjtu.edu.cn` / Message-ID 后缀硬编码 | 新增 `email.mx_domain`、`email.mx_host`、`email.message_id_domain` |
| **导出匿名化盐** | `export_data.py` 读未声明的 `EXPORT_SALT` | 新增 `export.salt`（可选·密钥）→ `EXPORT_SALT` |
| 端口/DB 路径/SMTP/收件人抄送/告警阈值冷却/备份保留远端/队列并发节流/webhook 开关重试/静默时段 | 已在（前几轮完成） | 保留并纳入总清单 |

**总计 180 个配置键**（时间项 55 个另见 `docs/时间项配置总表.md`）。

### 产物
| 产物 | 说明 |
|---|---|
| `docs/可配置项总清单.md` | **自动生成**，逐项列出 `键名 / 类型 / 默认值 / 作用(含注入的环境变量名) / 必填(含取值范围) / 生效方式(热加载或重启)`，按 19 个命名空间分组；`tools/gen_config_doc.py --check` 防漂移 |
| `services/common/lib_study.py` | 研究设计参数单一真源（带内置默认值 = 原硬编码值，缺文件不报错） |
| `config/study_calendar.example.yaml` | 干预时间轴/周次/目标完成度模板，`install.sh` 安装为 `study.calendar_path` |
| 控制台「**应用配置**」页 | `/api/appconfig`：按命名空间列出**全部 180 项**，可改、可预览、可恢复默认、写前自动备份、写审计；**密钥类只显示「已配置/未配置」，不回显、不可改**（只在 `secrets.env` 维护） |
| 控制台「**调度**」页 | `/api/schedule`：55 个时间项（上一轮） |

### 三方一致（用户要求）
| 方向 | 机制 | 结果 |
|---|---|---|
| schema ↔ yaml | `tools/appconfig.py --check-example-keys`（双向） | ✅ **180 键完全对应，无缺键、无悬空键** |
| yaml → 实际读取 | `ENV_MAP` 渲染 systemd 环境文件 | ✅ `config.env_bridge`：**151 个已声明环境变量覆盖全部服务侧读取（82 个）** |
| 实际读取 → yaml | 扫描 `os.environ.get("X")` 反向核对 | ✅ 未声明项 **0**（内部/派生/历史别名走显式允许清单并注明理由） |

### 配置完备性自检
* 启动即校验：`tools/appconfig.py --check`（缺必填 / 类型错 / 越界 / 未替换占位符 → **逐条指出点分键位置**）；
* 自检内置：`config.placeholder`、`config.schema`、`config.example_keys`、`config.time_format`、`config.env_bridge`、`config.secrets`；
* 控制台「应用配置」页顶部直接显示当前校验错误列表。

## 本轮（追加约束）验证证据
| 验证 | 结果 |
|---|---|
| `tools/verify_scoring_parity.py` | ✅ **1771 断言全等**（+4 条元数据差异已标注） |
| 自检 `--only parity` | ✅ 通过（并入自检，生产无权威库时优雅 skip） |
| `--check-example-keys` | ✅ 180 键一致 |
| `config.env_bridge` | ✅ 0 未声明 |
| `gen_config_doc.py --check` / `gen_time_config_doc.py --check` | ✅ 文档与配置一致 |
| 单元/集成测试 | ✅ **63 项**（新增 13 项：parity 3 + 三方一致 7 + 完备性 3） |
| `ci.sh --with-install` | ✅ 8/8 全绿 |
| `install.sh --dry-run` | ✅ 退出 0 且幂等 |

## 需用户确认（本轮新增）
1. **PSQI `睡眠问题` 题组含 13 子题**（含 C2B/C6/C7A/C7B）——已按问卷实测结构落地；
   若课题方法学另有一致性说明，请确认表述。
2. **`scoring.custom_bands_path`**：如需论文口径之外的分档切点，可放一份 YAML 覆盖；
   当前留空 = 用权威内置切点。
3. **研究设计日历**：`study_calendar.example.yaml` 的示例值 = 改动前硬编码值（行为不变）；
   换课题时请更新该文件。
4. **`export.salt`**：匿名化盐值建议在 `secrets.env` 设置（当前留空用内置默认）。


---

# 补做与复核记录（2026-09-16 续 · 二次复核）

> 本节记录对「追加约束 ①②」的**独立复核**结果与**差额修复**。复核以「实测状态」为准，
> 与上文结论一致的部分不重复。

## 1. 复核发现并修复的「装上即坏」缺陷（第 3 个）

| # | 缺陷 | 后果 | 修复 |
|---|---|---|---|
| 3 | `tools/verify_scoring_parity.py` 用**仓库布局**定位程序侧计分模块（`<PKG>/services/sjtu_survey_pro/survey_scoring.py`），但 `install.sh` 执行 `cp -r services/* $INSTALL_ROOT/` 会**抹平 `services/` 这一层** | 装到服务器后，自检 `parity` 组恒报「部署包计分模块不存在」→ **门禁在真实安装布局下失效** | 改为**双布局探测**（先仓库布局、再 `$INSTALL_ROOT/sjtu_survey_pro/...`）；补 `_find_program_scoring()` 并在真实沙箱布局下实测通过 |

**沙箱实测**（按 `install.sh` 的真实落点铺开）：`tools/selfcheck.py` → **通过 34 / 失败 0 / 跳过 8**，
其中 `parity` 组 ✅「程序与权威条目库逐条相同（1771 项断言全等；另有 4 条元数据差异）」。

## 2. 新增：可复现的配置穷尽审计器

* `tools/audit_config.py`：扫描 `services/`、`tools/`、`scripts/`、`systemd/`、`nginx/`、`install.sh`
  （**105 个文件**），逐条判定部署敏感字面量的外提状态；
  只看**代码部分**（剔除注释与文档字符串），避免文档性误报。
* 实测结果：命中 **50** 处 —— 已外提（读配置/环境变量）**25**、`${VAR}` 密钥引用 **3**、
  允许保留（安装器/模板/扫描器规则）**22**、**待外提 0**。
* 同时外提了 2 个开发用启动脚本的端口：`services/diet_survey/run_all.sh`、
  `services/sjtu_survey_pro/run_all.sh` → `${PORT_*:-默认值}`（与 systemd 环境变量同一命名）。
* 产物：`_测试证据_20260916g/配置审计小结.md` + `配置审计_明细.json`。

## 3. 校准文档

* 新增 `docs/分析口径一致性核对.md`：逐项列出**计分 / 饮食抽取与营养量化 / 完成度与依从性 /
  统计推断 / 三链路结构**与科研处理的比对与结论，并明确
  **「部署包不做科研级营养量化与推断统计」「完成度赋分口径 ≠ 科研 follow_rate，禁止混用」**。
* `services/common/lib_study.py`：修正完成度注释（原文「量表 3 次×3 分」与取值
  `scale_points_per_round=1, scale_max=10` 不符），并注明「本口径仅供控制台显示」。

## 4. 《可配置项总清单》补齐 8 列

`docs/可配置项总清单.md` 现含 **键名 / 类型 / 默认值 / 单位 / 作用 / 是否必填 / 生效方式 / 所属分组**；
「单位」由键名约定在生成器内**确定性推导**（`tools/gen_config_doc.py::_unit`），不手填、不会漂移。

## 5. 「改配置即生效」实测证据

改动 `schedule.survey_sync_minutes` 60→20、`health_monitor_minutes` 30→10、
`timer_randomized_delay_seconds` 60→90，重跑 `--render-timers`：

| timer | OnCalendar | RandomizedDelaySec |
|---|---|---|
| `research-survey-sync` | `*:0/60` → **`*:0/20`** | 60 → **90** |
| `research-health-monitor` | `*:0/30` → **`*:0/10`** | 60 → **90** |
| 其余 4 个 timer | 不变 | 60 → **90** |

证据：`_测试证据_20260916g/改配置即生效/`（`README.md` + `render_timers.diff` + 前后渲染产物）。

## 6. 二次复核验收（四项 + 附加）

| 验证 | 结果 |
|---|---|
| `tools/verify_scoring_parity.py` | ✅ 1771 断言全等，退出码 0（唯一差异 = PSQI `max_score` 展示用元数据） |
| `tools/selfcheck.py`（真实沙箱布局 + `AUTHORITY_SCORING_PATH`） | ✅ **34 通过 / 0 失败 / 8 跳过**（含 `parity`、`config.env_bridge`） |
| 全脚本 `bash -n` | ✅ 9 个脚本全通过 |
| `install.sh --dry-run` | ✅ 两次均退出 **0** 且输出**逐字节一致**（幂等） |
| schema ↔ `app.yaml.example` | ✅ **180 键**完全对应，无悬空键 |
| `gen_config_doc.py --check` / `gen_time_config_doc.py --check` | ✅ 文档与配置一致 |
| `tools/audit_config.py` | ✅ 待外提 **0** |
