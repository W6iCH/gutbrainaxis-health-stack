# 设计复查 — 问题清单（2026-09-16）

> 复查对象：`12_部署包_可发布/`（已发布 GitHub `W6iCH/gutbrainaxis-health-stack`）
> 复查维度：架构一致性 / 安全合规 / 健壮性 / 可维护性 / 运维可用性
> 严重度：🔴 高危（功能失效或凭据泄露）｜🟠 中（行为与文档/意图不符）｜🟡 低（可维护性/体验）
>
> **已修** = 本次已实际修改代码或配置；**未修** = 需外部决策或超出本包范围。

---

## 一、高危（🔴）

### P1. `install.sh` 不安装 `scripts/` 与 `tools/`，导致两个 timer 装上即失败 —— 已修
* **现象**：`install.sh` 只 `cp -r services/* $INSTALL_ROOT/`；但
  `systemd/research-health-monitor.service` 的 `ExecStart=__VENV__ __APP_BASE__/scripts/health_monitor.py --quiet`、
  `research-rclone-backup.service` 指向 `__APP_BASE__/scripts/rclone-backup.sh` ——
  这两个脚本**从未被安装**。同理自检/建库工具在 `tools/` 也缺失。
* **影响**：健康巡检与自动备份**静默失效**；`verify.sh` 引用的工具不存在。
* **修复**：`install.sh` 增加 `scripts/`、`tools/`、`docs/` 的复制；`selfcheck.py` 增加
  对这三个目录的运行时检查项（`paths.tool.*`）。

### P2. 问卷邮件队列写入必然抛异常（`conn.lastrowid`）—— 已修
* **现象**：`services/sjtu_survey_pro/survey_database.py::log_email()` 返回
  `conn.lastrowid`；`sqlite3.Connection` **没有** `lastrowid` 属性（已实测确认）。
* **影响**：每次创建邮件任务都抛 `AttributeError` → 邮件永远发不出；
  上层若吞异常则表现为「被试收不到报告」。
* **修复**：改用 `cur = conn.execute(...)`，`return cur.lastrowid`，并加注释说明。

### P3. 公开仓库中硬编码真实问卷 Token —— 已修
* **现象**：`services/exercise_survey/exercise_sync_cron.py` 硬编码
  `.../public/result/<REDACTED_已轮换>/json`。
* **影响**：凭据随公开仓库发布；任何人可拉取**真实运动答卷**（含学号/姓名/邮箱）。
* **修复**：改为读 `WJX_EXERCISE_TOKEN` 环境变量，占位符入 `app.yaml`；
  并在文件头标注原值与原因。⚠️ **该 Token 已在公开历史中，必须由课题方在问卷平台侧吊销/轮换。**

### P4. 控制台进程管理对多数服务不可用（白名单与单元名不一致）—— 已修
* **现象**：`service_manager.py::_systemd_map` 把 `webhook` → `research-webhook`、
  `diet_llm_queue` → `research-diet-llm`，而真实单元名为
  `research-diet-webhook` / `research-diet-llm-queue`；
  `data_dashboard`、`survey_sync`、`diet_sync`、`exercise_sync` 完全没有映射。
* **影响**：控制台「进程开关」对这些服务报 unit not found；数据看板根本不在白名单里。
* **修复**：白名单改为「单一真源」——每条目直接声明 `unit` + `kind`(service|timer|none)，
  覆盖全部 13 service + 7 timer；`status()` 返回 `unit`/`kind`，无单元服务标注说明。

### P5. 饮食问卷没有定时拉取（缺 systemd 单元）—— 已修
* **现象**：`diet_sync_cron.py` 存在，但 `systemd/` 下**没有** `research-diet-sync.{service,timer}`。
* **影响**：饮食数据只能靠 Webhook 被动触发；漏事件即永久丢数据。
* **修复**：新增 `research-diet-sync.service` + `.timer`（每 15 分钟，周期来自配置）。

---

## 二、中危（🟠）

### P6. 每日报告实现存在但从未排程（且有两份重复实现）—— 已修
* **现象**：`services/feedback/daily_report.py` 与 `services/diet_survey/daily_report.py`
  是两份高度重复的实现；**两者都没有 systemd 单元/timer**。
* **影响**：每日报告从未发送。
* **修复**：指定 `services/feedback/daily_report.py`（含 CC 抄送）为规范实现，
  新增 `research-daily-report.service` + `.timer`（默认 08:30）；
  另一份保留但标注 deprecated 且不再排程。

### P7. 配置分散且不联动，控制台改不到真实生效的项 —— 已修
* **现象**：`config/env.example`（约 20 组、上百项）与 `config/console.env.example`（3 项）
  并存；控制台 `ConfigManager.FIELDS` 只覆盖 15 项；`install.sh` 还**从未复制
  `console.env.example`**，控制台一直读不到配置。
* **影响**：改配置要靠手改 `/etc/research-app/env`；无 schema、无缺项检查。
* **修复**：引入**统一配置入口** `config/app.yaml`（108 键 + JSON Schema + 启动即校验），
  由 `tools/appconfig.py` 渲染出 `env`；`install.sh` 同时生成 `app.yaml` /
  `secrets.env`(0600) / `console.env`(600，随机口令)。

### P8. `install.sh` 的 dry-run 语义二义 —— 已修
* **现象**：定义了 `DRY_RUN_PREFIX` 却**从未使用**；`--dry-run --prefix=/tmp/x` 一边
  显示 `/etc/systemd/system/...` 等真实系统路径，一边声称不修改系统。
* **修复**：新增 `--dry-run-prefix=`（默认 `/tmp/gutbrainaxis-dryrun`）；
  dry-run 时所有系统级路径（`/etc/systemd`、`/etc/nginx`、`/etc/logrotate.d`、
  `/root/.config/rclone`、`/etc/research-app`）统一加前缀，输出即「将要写入的确切位置」。
  已实测两次 dry-run 输出完全一致（幂等）。

### P9. `verify.sh` 检查不存在的入口文件，且端口清单不全 —— 已修
* **现象**：检查 `admin_console/admin_console.py`（不存在，实际为 `app.py`）→ 永远报缺失；
  端口清单缺 `8090`（数据看板）；依赖 `ss`/`lsof`/`stat -c`，非 Linux 或精简系统会误报。
* **修复**：`verify.sh` 改为**薄封装**，调用 `tools/selfcheck.py`（与控制台同后端）；
  无自检引擎时回退到最小化 shell 检查（入口清单已修正）。

### P10. systemd 单元引用不存在的文档路径 —— 已修
* **现象**：4 个 timer 的 `Documentation=file://__APP_BASE__/docs/0X_*.md` 指向不存在的文件名。
* **修复**：统一改指实际存在的 `docs/数据模型与存储.md`、`常见故障排查.md`、
  `升级备份与回滚.md`；并把 `docs/` 纳入安装。

### P11. 人群基线数据自相矛盾，且硬编码疑似个体数据 —— 已修
* **现象**：
  * `.example` 说明写「示例值」，数值却是 `mean=0, sd=1`（正态曲线毫无意义），
    且 DEBQ 写 `min=0,max=4`（真实量程 1–5）。
  * `population_charts.make_debq_2d_svg()` **硬编码 4 个「人群核」坐标**
    （如 `(1.92, 2.67, 1.0)`），疑似个体/小组数据残留。
  * 旧 `population_stats.json`（n=56）与内置计分口径不一致 → 百分位失真，
    却被静默优先加载。
* **修复**：生成**内置默认数据** `population_stats.default.json`（基于 226 份原始问卷的
  **匿名聚合**，按口径分块）；`make_debq_2d_svg` 改为由聚合 mean/sd 导出的二元正态密度；
  旧的本地覆盖重命名为 `population_stats.legacy.json`（保留但不自动加载）。

### P12. 计分口径与基线口径可能不一致（无开关）—— 部分修复
* **现象**：`14_评分标准/` 已裁定「原生编码口径（Edition B）」为唯一依据，
  且已指出已发布 `survey_scoring.py` 的 PSS-14/PSQI/WHOQOL 缺陷；
  但部署包只实现了旧口径，无版本开关。
* **影响**：报告分数与权威口径不符；同侪百分位取决于基线用哪套口径。
* **修复（本包范围内）**：内置基线**同时**包含两个口径块，`population_charts`
  按 `SCORING_VERSION`（新配置项 `app.scoring_version`）选择匹配块，避免
  「分数一套口径、基线另一套口径」的静默错配；默认推荐 `3.0-native-ordinal`。
* **未修**：把 `survey_scoring_native.py` 合并进 `survey_scoring.py`（方式 A/B）
  属**计分语义变更**，`14_评分标准/03_条目库_修正/与已发布版本差异.md` 明确标注
  「需授权后合并」，本次不擅自改动。

### P13. 服务日志目录不存在时启动即崩 —— 已修
* **现象**：`diet_llm_queue.py` 只创建队列目录，`logging.FileHandler(logs/llm_queue.log)`
  在 `logs/` 缺失时抛 `FileNotFoundError` → worker 起不来（systemd 重启循环）；
  同类问题存在于 `diet_feedback_server.py`（触发器日志）。
* **修复**：两处在模块加载时 `os.makedirs(..., exist_ok=True)`。

### P14. 无 `/healthz`、无优雅停机、无日志轮转 —— 已修
* **现象**：各服务只有零散 `/health`（部分仅 `/`）；`SIGTERM` 直接终止，可能截断
  SQLite 写入或丢失内存队列状态；`logs/` 无轮转，长期运行撑满磁盘。
* **修复**：统一 `/healthz`（含 DB 可读性）；`services/common/lib_runtime.py` 提供
  信号处理与统一探针体；新增 `/etc/logrotate.d/research-app`（按天、保留 14 份、copytruncate）。

---

## 三、低危（🟡）

### P15. 工作树残留 `.DS_Store` 与 `__pycache__` —— 已修
虽被 `.gitignore` 排除（未入库），但会随打包/拷贝扩散。已在包内清理，
并在 `install.sh` 增加安装后清理步骤。

### P16. `DRY_RUN_PREFIX` / 未使用变量、`sed -i ''` 的 macOS/GNU 兼容绕行 —— 已修
统一改为「先试 `.bak` 后缀形式，失败再试 GNU 形式」，并删除 `*.bak`。

### P17. `run_all.sh`（sjtu_survey_pro）输出与实际不符 —— 已修（注释层）
脚本打印「邮件流程已移除（v2）」，但仓库实际存在完整邮件链路
（`email_feedback.py` + `email_log` 表 + 每日报告）。已在脚本头注释标注该误导，
生产以 systemd 为准（`run_all.sh` 仅供本地手动调试）。

### P18. GitHub 推送后本地修改未同步 —— 未修（需用户决策）
本次改动均在**本地工作树**，未 `git commit` / `git push`；
是否推送、是否公开、LICENSE 授权确认，需课题方决策后执行。

---

## 四、复查结论

| 维度 | 复查前 | 复查后 |
|---|---|---|
| 安装完整性（单元 ExecStart 目标是否被安装） | 🔴 2/13 单元指向不存在脚本 | ✅ 13/13 |
| 计分/报告链路（邮件队列可写） | 🔴 必抛异常 | ✅ 已修并端到端验证 |
| 凭据泄露 | 🔴 1 处真实 Token 硬编码 | ✅ 已改为环境变量（**需轮换 Token**） |
| 控制台进程管理可用性 | 🟠 4/8 服务不可用 | ✅ 覆盖 20 个单元 |
| 定时任务覆盖（拉取/日报/备份/巡检） | 🟠 缺 2 类 | ✅ 7 个 timer 全覆盖 |
| 配置单一入口 + 校验 | 🟠 无 | ✅ app.yaml + schema + 启动校验 |
| 自检 | 🟠 无结构化自检 | ✅ 12 组 40+ 检查项，JSON+表+退出码 |
| 运行期健壮性（健康探针/停机/轮转） | 🟠 无 | ✅ 已补 |
| 二次开发基线（测试/模拟数据） | 🟠 无 | ✅ seed/clear + 证据 |

**仍未解决（需外部决策）**：
1. 公开仓库历史中的真实问卷 Token **必须在问卷平台侧轮换**（代码已改，历史已泄）。
2. 是否把权威「原生编码口径」合并进部署包计分核心（影响所有历史分数与结论）。
3. LICENSE 仍为占位，发布前需权利人确认。
4. 本地改动尚未 commit/push。
